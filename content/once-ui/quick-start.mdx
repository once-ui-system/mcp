---
title: "Quick start"
summary: "Once UI is an open-source design system built for indie creators, small teams, and startups to fuel the next generation of web applications."
image: "/images/docs/once-ui.jpg"
updatedAt: "2025-03-17"
navLabel: "Quick start"
navIcon: "learn"
---

## About

Once UI is an open-source design system built for indie creators, small teams, and startups to fuel the [next generation](https://lorant.one/work/once-ui) of web applications.

## License

Once UI is licensed under the MIT license.

## Quick start

Create a new Once UI app with our CLI tool. It will clone the latest version and install the dependencies:

<CodeBlock compact marginBottom="16" codes={[
  {
    code: "npx create-once-ui-app@latest my-app",
    language: "bash"
  }
]} />

Alternatively, you can clone the repository and install dependencies manually:

<CodeBlock compact marginBottom="16" codes={[
  {
    code: "git clone https://github.com/once-ui-system/nextjs-starter.git",
    language: "bash"
  }
]} />

Install dependencies:

<CodeBlock compact marginBottom="16" codes={[
  {
    code: "cd my-app && npm install",
    language: "bash"
  }
]} />

Start the local development server:

<CodeBlock compact marginBottom="16" codes={[
  {
    code: "npm run dev",
    language: "bash"
  }
]} />

## Structure

We recommend using Once UI as a self-contained entity in your mono-repo structure. Alternatively, if you're building a larger ecosystem, you can serve your own Once UI package through npm. We're planning to distribute Once UI as a package with the upcoming 1.0 version.

<CodeBlock compact marginTop="16" codes={[
  {
    code:
`.
├── once-ui
│   ├── tokens
│   │   ├── index.scss
│   │   └── ...
│   ├── styles
│   │   ├── index.scss
│   │   └── ...
│   ├── components
│   │   ├── Accordion.tsx
│   │   ├── Accordion.module.scss
│   │   └── ...
│   ├── types.ts
│   └── icons.ts
├── app-1
│   ├── resources
│   │   └── config.js
│   ├── page 1
│   │   └── page.tsx
│   ├── page 2
│   │   └── page.tsx
│   ├── layout.tsx
│   ├── page.tsx
│   └── utils
├── app-2
│   └── ...
├── next.config.js
├── package.json
└── tsconfig.json`,
    language: "bash"
  }
]} />